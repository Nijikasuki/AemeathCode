# 项目记忆

> 这是「项目记忆」模板,类似 Claude Code 的 `CLAUDE.md` —— 人写的项目背景,会**常驻**
> 拼进 system prompt,让 agent 一上来就懂你的项目约定。
> 复制到 `.aemeath/AEMEATH.md` 并按你的项目改写即可。

## 这个项目是什么
一句话说清项目做什么、给谁用。

## 技术栈与约定
- 语言 / 框架:...
- 包管理:...(如 uv / poetry / npm)
- 跑测试:...(如 `uv run pytest`)
- 代码风格:...(缩进、命名、是否有 lint)

## 目录结构速览
- `src/` —— ...
- `tests/` —— ...

## 雷区 / 约定
- 提交前必须跑测试。
- 密钥放 `.env`,永不提交。
- 别改自动生成的文件(如 `xxx`)。
